name: VibeCop

on:
  pull_request:
    branches: [main, master]
  push:
    branches: [main, master]

jobs:
  vibecop:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install dependencies
        run: npm ci

      - name: Build VibeCop CLI
        run: npm run build

      - name: VibeCop scan
        run: node packages/cli/dist/index.js . --severity error
