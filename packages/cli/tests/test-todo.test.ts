import { describe, it, expect } from 'vitest'; import { parse } from '@typescript-eslint/typescript-estree'; import fs from 'fs'; import rule from '../src/scanner/rules/ai-smells/todo-stub-functions.js'; describe('test', () => { it('runs', () => { const src = fs.readFileSync('tests/fixtures/should-flag/auth-fixture-buggy.ts', 'utf8'); const ast = parse(src, { loc: true, range: true, comment: true, jsx: false }); const findings = rule.check(ast as any, src, 'test.ts'); console.log(findings); }); });
